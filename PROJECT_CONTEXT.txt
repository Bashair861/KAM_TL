================================================================================
  AETHER KAM — PROJECT CONTEXT FILE
  Purpose: Copy-paste this entire file into a new chat to give full context.
  Keep this file updated after every major phase.
  Last updated: Phase 4.5 - Salesforce Integration (lookup + sync + field-mapping modal)
================================================================================

────────────────────────────────────────────────────────────────────────────────
WHAT IS THIS PROJECT?
────────────────────────────────────────────────────────────────────────────────
Aether KAM is an internal Key Account Management (KAM) portal for an IT services
company. Used by the KAM team to manage enterprise client relationships, track
account health, manage escalations, handle contracts, and grow accounts.

The app is built as a learning/portfolio project. It started fully static
(all data hardcoded in a JS file). It is now progressively wired to a real
Supabase backend with full read AND write operations.

The active working copy is currently at:
  C:\Users\majid.zia\Documents\GitHub\KAM_TL


────────────────────────────────────────────────────────────────────────────────
TECH STACK
────────────────────────────────────────────────────────────────────────────────
- React 19 + JavaScript (ESM — project was converted from TypeScript to JS)
- TanStack Router v1 (file-based routing) + TanStack Start
- TanStack Query v5 (useQuery / useMutation for all data operations)
- Supabase (PostgreSQL database + Auth)
- Tailwind CSS v4
- Vite 7 (build tool)
- Lucide React (icons)
- Radix UI (headless components)
- Recharts (charts)
- bun (preferred package manager + script runner)
- Deployed target: Cloudflare Pages (via @cloudflare/vite-plugin)

NOTE: The project was originally TypeScript (.ts/.tsx). It has been converted
to plain JavaScript (.js/.jsx). All files under src/ use .js or .jsx extensions.


────────────────────────────────────────────────────────────────────────────────
FOLDER STRUCTURE (key files only)
────────────────────────────────────────────────────────────────────────────────
src/
  routes/
    __root.jsx              ← Root layout: AuthProvider + AppShell + redirect logic
    index.jsx               ← Dashboard page
    login.jsx               ← Login page (no sidebar)
    accounts.index.jsx      ← Accounts portfolio list + KAM assignment column
    accounts.$accountId.jsx ← Account detail (Client 360) — largest file, 7 tabs
    contracts.jsx           ← Contracts page (dynamic, Supabase-backed)
    strategy.jsx            ← Strategy builder page (still partially static)
    escalations.jsx         ← Escalations list page
    educate.jsx             ← Education page

  components/
    layout/
      AppSidebar.jsx        ← Main navigation sidebar (shows real user from useAuth)

  context/
    AuthContext.jsx         ← React context: session, profile, loading state

  services/
    auth.js                 ← signIn(), signOut() using Supabase Auth
    db.js                   ← All Supabase query + mutation functions

  data/
    kam-data.js             ← TypeScript types converted to JSDoc + ROLE_PERMISSIONS
                              + static mock data (used as reference / fallback only)

  lib/
    supabase.js             ← Supabase client (VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY)

  db/
    schema.sql              ← Full PostgreSQL schema (13 tables + enums + RLS)
    reset.sql               ← Drops all tables/enums (run before re-seeding from scratch)
    enable-rls.sql          ← Enables RLS + adds public SELECT policies on all tables
    grant-read.sql          ← Grants SELECT to anon + authenticated roles
    seed.js                 ← Idempotent seed script (bun run seed)
    setup-auth.sql          ← Auth trigger + UPDATE statements for all 3 users
    add-kam-assignment.sql         ← Migration: adds assigned_kam_id to accounts + RLS policy
    add-kpi-data.sql               ← Migration: adds kpi_data (jsonb) to health_scores + policies
    add-account-history.sql        ← Migration: creates account_history audit log table
    add-account-insert-policy.sql  ← Migration: INSERT RLS policy on accounts table
    add-health-scores-insert-policy.sql ← Migration: INSERT RLS policy on health_scores
    add-account-linkedin-url.sql   <- Migration: adds linkedin_url to accounts
    add-linkedin-summary.sql       <- Migration: adds linkedin_summary to accounts
    add-linkedin-summary-updated-at.sql <- Migration: tracks LinkedIn summary timestamp
    add-account-website-summary.sql <- Migration: adds website URL + AI website summary fields

  styles.css                ← Global Tailwind CSS styles + custom design tokens

.env.local                  ← Environment variables (gitignored — NEVER commit)


────────────────────────────────────────────────────────────────────────────────
Recent JS additions:
  src/routes/users.jsx              <- Head of KAM user management page
  src/routes/set-password.jsx       <- Invite/password setup page
  src/services/user-admin.js        <- Server-only Supabase Auth invite function
  src/db/add-user-management.sql    <- profiles.is_active + user management RPCs/RLS
  src/db/rename-ceo-role.sql        <- keeps user_role enum value as CEO
  src/db/add-account-linkedin-url.sql <- accounts.linkedin_url for Salesforce Account LinkedIn
  src/db/add-linkedin-summary.sql   <- accounts.linkedin_summary for AI-generated LinkedIn brief
  src/db/add-linkedin-summary-updated-at.sql <- accounts.linkedin_summary_updated_at timestamp
  src/db/add-account-website-summary.sql <- accounts.website_url + website_summary fields
  src/services/linkedin-summary.js  <- Server fn: generates/saves Account's Linkedin Summary
  src/services/website-summary.js   <- Server fn: generates/saves Account's Website Summary

Salesforce integration additions (Phase 4.5):
  src/services/salesforce.js        <- Server fn: lookupSalesforceAccountBundle
                                       Searches SF by account name, returns account + contacts
                                       Auth: OAuth Client Credentials flow (CLIENT_ID + CLIENT_SECRET)
  src/services/salesforce-sync.js   <- Server fn: syncSalesforceMappedFieldsServer
                                       Writes selected fields to Supabase accounts + stakeholders
                                       Uses service role key to bypass RLS
  SalesforceMappingModal            <- Inline component in accounts.$accountId.jsx
                                       Field-by-field review table with checkboxes
                                       Groups: Account/KYC + one group per Salesforce Contact
  Account Details section           <- Overview tab section below KYC for LinkedIn URL

USER ROLES & PERMISSIONS (ROLE_PERMISSIONS in kam-data.js)
────────────────────────────────────────────────────────────────────────────────
Three roles exist:

  CEO          → read: true,  write: false, scope: "all"
  Head of KAM  → read: true,  write: true,  scope: "all"
  KAM          → read: true,  write: true,  scope: "assigned"

- "scope: all"      = sees and can edit all accounts
- "scope: assigned" = KAM only sees accounts where assigned_kam_id = their profile id
- Write access controls whether Save/Edit/Add buttons are enabled in the UI
- Role is stored in the `profiles` table in Supabase
- Role is loaded into React via AuthContext → useAuth() → profile.role

Seeded users (3 total):
  julian@aether.io  → Julian Drake,  Head of KAM (read/write all accounts)
  maya@aether.io    → Maya Lin,      KAM (read/write assigned accounts only)
  bashair@aether.io → Bashair Ahmad, KAM (read/write assigned accounts only)

RULE: Head of KAM assigns accounts to KAMs only (not to themselves or CEO).
      fetchKamUsers() enforces this by only querying profiles WHERE role = 'KAM'.


────────────────────────────────────────────────────────────────────────────────
WHAT HAS BEEN DONE — PHASE HISTORY
────────────────────────────────────────────────────────────────────────────────

PHASE 0 — Static prototype
  - App existed with all data hardcoded in src/data/kam-data.js
  - No database, no auth, no API calls
  - All pages rendered mock data directly from JS objects

PHASE 1 — Supabase database setup + live data reads
  ✓ Designed PostgreSQL schema (13 tables) in src/db/schema.sql
  ✓ Created reset.sql, enable-rls.sql, grant-read.sql helper scripts
  ✓ Created seed.js — idempotent upsert script (bun run seed)
      2 profiles, 5 accounts, 11 stakeholders, 40 health_scores,
      ~150 health_metrics, 5 contract_details, 14 activities,
      20 retention_growth, 6 education_log, 3 escalations,
      8 escalation_action_items, 5 opportunities, 5 notifications
  ✓ Created src/services/db.js with all query functions
  ✓ Wired all app pages to read live data from Supabase via useQuery
  ✓ Mapper pattern: snake_case DB → camelCase JS objects
  ✓ Fixed RLS blocking reads (enable-rls.sql)
  ✓ Fixed service role key exposure (never in VITE_ variables)

PHASE 2 — Authentication (Supabase Auth)
  ✓ Created src/services/auth.js (signIn, signOut)
  ✓ Created src/context/AuthContext.jsx
      - loadProfile(): UUID lookup first, email fallback second
  ✓ Updated __root.jsx: wrapped in AuthProvider, redirect logic in AppShell
  ✓ Created login.jsx: dark card, email + password, spinner + error states
  ✓ Updated AppSidebar.jsx: shows real user name/role + sign out button
  ✓ All components use useAuth() for role-based edit gating

PHASE 2.5 — KAM Account Assignment
  ✓ accounts.assigned_kam_id (text FK → profiles.id) added via add-kam-assignment.sql
  ✓ fetchAccounts() filters by assigned_kam_id when role === "KAM"
  ✓ fetchKamUsers() returns only KAM-role profiles for assignment dropdown
  ✓ updateAccountKam(accountId, kamId) — persists assignment to DB
  ✓ Assignment dropdown in accounts list page (Head of KAM only)
  ✓ Assignment section in account detail Overview tab
  ✓ All fetchAccounts() callers use user-scoped queryKey: ["accounts", userId, role]
  ✓ Bashair Ahmad added as 3rd KAM user (bashair@aether.io)

PHASE 3 — Write Operations (partial — in progress)
  ✓ KYC fields in Overview tab persist to Supabase on Save
      - updateAccountKyc() updates accounts table
      - Fixed field doubling bug: each field initialised from its own DB column only
      - Dirty state tracking: savedSnapshot + isDirty comparison
      - Fixed save bar: fixed bottom positioning, auto-hides 3 seconds after save
  ✓ KPI Editor (Score Marking Matrices tab) persists to Supabase
      - updateHealthBlock() updates health_scores.score + kpi_data + health_metrics
      - kpi_data (jsonb) stores full section/field state including checkbox states
      - On re-open: loads from kpiData if present (restores exact saved state)
      - Score scaled: 0-5 internal → 0-10 DB value
      - add-kpi-data.sql adds kpi_data column + UPDATE policies
  ✓ Client History audit log (new "Client History" tab in account detail)
      - New account_history table: id, account_id, field_name, old_value, new_value,
        edited_by (name text), edited_at (timestamptz)
      - account_history.account_id is type TEXT (matches accounts.id which is text)
      - logAccountChanges() inserts rows on every save
      - fetchAccountHistory() fetches ordered by edited_at DESC
      - Logging wired to: KYC save, KAM assignment, KPI score save
      - add-account-history.sql creates the table + RLS policies
  ✓ Contracts page fully dynamic
      - fetchContracts() joins accounts + contract_details in one Supabase query
      - Shows: Account, Type, Duration, Value, Renewal, Process Compliance
      - Role-based filtering (KAM sees only assigned accounts)
      - Account name links to detail page
  ✓ New Account creation (Head of KAM only)
      - "+ New Account" button visible only to Head of KAM role
      - NewAccountModal: name, short code (auto-derived), industry, region, tier,
        contract type, contract value, ARR, renewal days, primary contact, assign KAM
      - Account ID auto-generated as slug from name (e.g. "Helios Robotics" → "helios-robotics")
      - createAccount() inserts into accounts table with sensible defaults
        (health: 50, status: healthy, trend: 0, retention_risk: Low)
      - add-account-insert-policy.sql adds INSERT RLS policy on accounts
      - On success: query cache invalidated, new account appears in list immediately
      - Duplicate ID shows inline error from Supabase
  ✓ KPI Editor — new account support + UX polish
      - New accounts (no health_metrics rows) now get a default seeded section
        so the modal is never empty
      - "Add KPI section" button in modal footer — adds a new blank section
      - Delete section button (Trash2) in every section header — removes section
      - updateHealthBlock() now upserts: INSERTs health_scores row if none exists
        (previously threw on new accounts), UPDATEs if row exists
      - add-health-scores-insert-policy.sql adds INSERT RLS policy on health_scores
      - fetchAccount() block() helper: when health_metrics is empty but kpi_data
        exists, derives metric display bars from saved KPI sections so score
        bars appear correctly for new accounts
      - Save failure now shows an error banner in the modal footer (was silent)


────────────────────────────────────────────────────────────────────────────────
PHASE 4 - User Management + Auth Invites + Contracts Stabilization
  - Re-applied the JS/JSX version of the user-management work after the project
    was converted from TypeScript to JavaScript.
  - Added a Users section in AppSidebar, visible to Head of KAM only.
  - Added /users route:
      src/routes/users.jsx
      Head of KAM can view all users, create users, change roles, and
      activate/deactivate users.
  - Added profiles.is_active support:
      AuthContext signs inactive users out.
      fetchKamUsers() excludes inactive KAMs from assignment dropdowns.
  - Added user-management service functions in src/services/db.js:
      fetchUsers()
      createUserProfile()
      updateUserRole()
      updateUserStatus()
  - Added server-only Supabase Auth admin invite flow:
      src/services/user-admin.js
      createManagedAuthUser() uses createServerFn() so the service role key
      stays server-side.
      Head of KAM creates a user, Supabase sends an invite email, the user
      clicks the email link, lands on /set-password, creates a password, then
      signs in normally.
  - Added /set-password route:
      src/routes/set-password.jsx
      Calls supabase.auth.updateUser({ password }) for invite/recovery sessions.
  - Added/updated SQL:
      src/db/add-user-management.sql
        Adds is_active, Head of KAM permission RPC, profile role/status RPCs,
        and RLS policies for profile insert/update.
      src/db/rename-ceo-role.sql
        Keeps the enum role name as CEO and migrates C Level back to CEO.
  - Role naming was changed back to CEO:
      ROLE_PERMISSIONS now uses CEO, Head of KAM, KAM.
      normalizeRole() maps legacy C Level rows to CEO to avoid crashes.
  - Contract duration mapping fixed:
      fetchContracts() handles Supabase contract_details as either an array
      or a single object, then maps duration to the Contracts page.
  - Contracts page crash fixed:
      Missing/null values such as contract_compliance now render as "-" instead
      of calling toFixed() on null and triggering "This page didn't load".
      Contracts route also shows an inline query error message instead of
      sending the whole app to the root error boundary.
  - Verification performed after this phase:
      npm.cmd run build passed.
      Targeted ESLint checks on touched JS/JSX files passed with the repo's
      Prettier CRLF rule disabled.
      /users, /set-password, and /contracts returned HTTP 200 locally.


IMPORTANT DECISIONS & PATTERNS
────────────────────────────────────────────────────────────────────────────────

1. DATA LAYER PATTERN (db.js)
   - All Supabase calls live in src/services/db.js
   - Mapper functions convert snake_case DB → camelCase JS objects
   - fetchAccount() uses Promise.all for 7 parallel queries (performance)
   - Components never import from @supabase/supabase-js directly
   - Mutations (useMutation) call db.js functions and also call logAccountChanges()
     in the same mutationFn so the history entry is part of the same operation

2. AUTH PATTERN
   - AuthContext is the single source of truth for auth state
   - session = Supabase Session object (null if logged out)
   - profile = row from profiles table (loaded by UUID or email fallback)
   - loading = true until first session check completes
   - Route protection is centralized in AppShell in __root.jsx

3. PERMISSION PATTERN
   - Every component that needs edit-gating: const { profile } = useAuth()
   - Then: const editable = ROLE_PERMISSIONS[profile?.role ?? "KAM"].write
   - Buttons use disabled={!editable}
   - "KAM" fallback = unknown roles default to most restrictive write scope

4. ENVIRONMENT VARIABLES
   - VITE_SUPABASE_URL      → browser-safe, used by src/lib/supabase.js
   - VITE_SUPABASE_ANON_KEY → browser-safe anon/publishable key
   - SUPABASE_SERVICE_ROLE_KEY -> server/Node only, bypasses RLS
     Used by seed.js and src/services/user-admin.js to create Supabase Auth
     invite users. NEVER put the service role key in a VITE_ variable.
   - File must be named .env.local (Vite convention — .env alone does not work)

5. KYC FIELD INITIALIZATION RULE
   - Each editable KYC field is initialized from ONE DB column only
   - Example: revenue field = account.revenue (not "account.revenue · ARR: $X")
   - Combining multiple columns into a single editable field caused doubling on save
   - If supplementary info needs showing, display it as read-only text separately

6. HISTORY LOGGING PATTERN
   - KYC save: computes diff between savedSnapshot and fields, logs changed fields
   - KAM assignment: logs "Assigned KAM" with old KAM name → new KAM name
   - KPI score: logs "Score: [title]" with old block.score → new score
   - editedBy is always profile?.name ?? "Unknown" (display name, not UUID)

7. account_history TABLE TYPE NOTE
   - accounts.id is type TEXT (not UUID) — it uses string IDs like "starlight"
   - Therefore account_history.account_id must also be TEXT
   - This is different from most other FK columns which use uuid

8. KPI DATA STORAGE
   - health_scores.kpi_data (jsonb) stores the full KpiSection[] array
   - On KpiEditorModal open: loads from block.kpiData if present, else seeds from metrics,
     else seeds one default section (new accounts with no metrics)
   - On save: upserts health_scores row (INSERT if new, UPDATE if exists) + full sections
     array saved to kpi_data. Existing metric rows updated by UUID if present.

9. NEW ACCOUNT DEFAULTS
   - health: 50, trend: 0, status: 'healthy', retention_risk: 'Low'
   - growth_upside: 0, white_space_count: 0, is_startup: false
   - No health_scores, health_metrics, or contract_details rows created on insert
   - KPI editor handles missing rows by inserting on first save
   - Score bars derived from kpi_data jsonb when health_metrics rows are absent

10. DERIVED METRIC BARS PATTERN
    - fetchAccount block() helper checks: if health_metrics is empty AND kpi_data exists,
      compute each section's score as (checked weight ÷ total weight) × 10
      and use section name as label → passed to mapHealthBlock as derived metrics
    - These show as progress bars in ScoreBlock without needing DB metric rows
    - Derived metrics have id: "derived-N" (not real UUIDs) — do not use for updates


────────────────────────────────────────────────────────────────────────────────
WHAT IS NEXT — UPCOMING PHASES
────────────────────────────────────────────────────────────────────────────────

PHASE 3 (remaining) — Write operations still to do
  - Activities tab: add/edit/delete activities (currently read-only)
  - Escalations: create/close/update escalations (currently read-only)
  - Education log: add new education entries (currently read-only)
  - Strategy page: still fully static

NOTE: As more data gets wired, all of it must be filtered by the KAM's assigned
accounts. Head of KAM and CEO always see everything unfiltered.

PHASE 4.5 (remaining) — Salesforce AI mapping assistant
  - Add src/services/ai-mapping.js: server function calling Google Gemini free API
    Takes the mapping rows (with current values + SF values) and returns
    { [rowId]: boolean } recommending which rows to pre-check
  - Add "✨ AI Suggest" button to SalesforceMappingModal header
    Triggers the AI call, shows spinner while pending
    Updates checkbox selections with AI recommendations
    User can still override any checkbox before syncing
  - Add AI badge next to checkbox on AI-recommended rows
  - Uses GEMINI_API_KEY env var (Google Gemini free tier — no cost)
  - AI prompt scope is strictly: field mapping only (no other purpose)

PHASE 5 - AI Assistant
  - Added Account's Linkedin Summary section in account Overview tab
      - Uses accounts.linkedin_url as input
      - Calls Gemini or OpenAI server-side via src/services/linkedin-summary.js
      - Saves 2-3 paragraph output to accounts.linkedin_summary
      - Saves the last generated time to accounts.linkedin_summary_updated_at
      - Focus metrics: new job postings, company major activities,
        competitor information, CEO status / communication updates
  - Added Account's Website Summary section below LinkedIn Summary
      - Uses accounts.website_url as input
      - Calls OpenAI server-side via src/services/website-summary.js
      - Saves 2-3 paragraph output to accounts.website_summary
      - Saves the last generated time to accounts.website_summary_updated_at
  - Wire src/services/ai.js to Google Gemini API
  - AI bot that answers questions about an account based on its data
  - Suggested entry: account detail page

PHASE 6 - Production hardening
  - Remove debug error banner from dashboard
  - Add proper loading skeletons
  - Error boundaries per-page
  - Deploy to Cloudflare Pages
  - Set environment variables in Cloudflare dashboard

PHASE 7 - Additional features (ideas, not committed)
  - Notifications center (currently static mock data in sidebar)
  - Real-time updates via Supabase Realtime
  - PDF export of account reports (pptxgenjs already installed)


────────────────────────────────────────────────────────────────────────────────
SUPABASE PROJECT INFO
────────────────────────────────────────────────────────────────────────────────
Project URL: https://ogkxkfhigejggnoobypk.supabase.co
Dashboard:   https://supabase.com/dashboard/project/ogkxkfhigejggnoobypk
Anon key:    stored in .env.local as VITE_SUPABASE_ANON_KEY
Service key: stored in .env.local as SUPABASE_SERVICE_ROLE_KEY

To set up on a new machine:
  1. Clone repo + git checkout Bashair
  2. bun install
  3. Create .env.local — get values from team lead via password manager (never GitHub):
       VITE_SUPABASE_URL
       VITE_SUPABASE_ANON_KEY
       SUPABASE_SERVICE_ROLE_KEY
       SALESFORCE_INSTANCE_URL
       SALESFORCE_LOGIN_URL
       SALESFORCE_CLIENT_ID
       SALESFORCE_CLIENT_SECRET
  4. bun run dev
  5. In Supabase Auth URL Configuration, add /set-password redirect URLs
  (DB is already set up on Supabase — shared across all machines)
  (Salesforce Connected App is already set up — no additional SF config needed)

To set up a FRESH Supabase project from scratch:
  1. Run schema.sql → enable-rls.sql → grant-read.sql in SQL Editor
  2. Add SUPABASE_SERVICE_ROLE_KEY to .env.local
  3. bun run seed
  4. Create auth users in Supabase: julian@, maya@, bashair@ (all @aether.io)
  5. Run setup-auth.sql in SQL Editor
  6. Run add-kam-assignment.sql in SQL Editor
  7. Run add-kpi-data.sql in SQL Editor
  8. Run add-account-history.sql in SQL Editor
  9. Run add-account-insert-policy.sql in SQL Editor
  10. Run add-health-scores-insert-policy.sql in SQL Editor
  11. Run rename-ceo-role.sql in SQL Editor
  12. Run add-user-management.sql in SQL Editor
  13. Run add-account-linkedin-url.sql in SQL Editor
  14. Run add-linkedin-summary.sql in SQL Editor
  15. Run add-linkedin-summary-updated-at.sql in SQL Editor
  16. Run add-account-website-summary.sql in SQL Editor


────────────────────────────────────────────────────────────────────────────────
KNOWN ISSUES / NOTES FOR NEXT SESSION
────────────────────────────────────────────────────────────────────────────────
- Activities, Escalations, Education Log tabs are still read-only (no write ops)
- Notifications in sidebar are still static mock data from kam-data.js
- Strategy page is still fully static
- User management has server-side authorization via current_user_is_head_of_kam()
  and a TanStack Start server function. Most other write areas still rely on UI
  guards plus table-level RLS policies.
- Debug error banner on dashboard (index.jsx) — kept intentionally for now
- Salesforce AI mapping assistant not yet built (Phase 4.5 remaining item above)
- Salesforce Connected App must have "Enable Client Credentials Flow" checked
  and a "Run As" user set (Setup → App Manager → Manage → Edit Policies).
  This is a one-time Salesforce admin step, not a code change.

================================================================================
END OF PROJECT CONTEXT FILE
================================================================================
