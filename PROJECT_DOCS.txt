================================================================================
  AETHER KAM — PROJECT DOCUMENTATION
  Purpose: Technical reference for database, configs, setup, and architecture.
  Keep this file updated after every major phase.
  Last updated: Phase 4.5 - Salesforce Integration (lookup + sync + field-mapping modal)
================================================================================


────────────────────────────────────────────────────────────────────────────────
1. TECH STACK VERSIONS
────────────────────────────────────────────────────────────────────────────────
React                    19.2.0
JavaScript (ESM)         — project converted from TypeScript to JS
Vite                     7.3.1
TanStack Router          1.168.25
TanStack Start           1.167.50
TanStack React Query     5.83.0
@supabase/supabase-js    2.106.2
Tailwind CSS             4.2.1
Lucide React             0.575.0
bun                      (preferred runner — npm run also works)
dotenv                   17.4.2  (used in seed.js)

NOTE: All source files use .js or .jsx extensions. TypeScript has been removed.


────────────────────────────────────────────────────────────────────────────────
2. ENVIRONMENT VARIABLES
────────────────────────────────────────────────────────────────────────────────
File: .env.local  (root of project, gitignored — NEVER commit to git)
NOTE: Must be named .env.local — Vite does NOT load plain .env by default.
NOTE: Share with teammates via password manager only — never via GitHub or chat.

  VITE_SUPABASE_URL=https://ogkxkfhigejggnoobypk.supabase.co
  VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  SUPABASE_SERVICE_ROLE_KEY=<service role key - server/Node only>

  # Salesforce — OAuth Client Credentials (server-only, no VITE_ prefix)
  SALESFORCE_INSTANCE_URL=https://orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com
  SALESFORCE_LOGIN_URL=https://orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com
  SALESFORCE_CLIENT_ID=<Connected App Consumer Key>
  SALESFORCE_CLIENT_SECRET=<Connected App Consumer Secret>

  # AI mapping assistant — Gemini free tier (server-only, no VITE_ prefix)
  # Get free key at: aistudio.google.com → Get API Key
  GEMINI_API_KEY=<your Gemini API key>

  # Account LinkedIn summary can use Gemini or OpenAI (server-only)
  OPENAI_API_KEY=<your OpenAI API key>
  OPENAI_MODEL=gpt-5.4-mini

Rules:
  - VITE_ prefix = exposed to browser. OK for anon/publishable keys only.
  - No VITE_ prefix = server/Node.js only. Required for all secret keys.
  - Service role key bypasses all RLS. Used only on the server/Node side:
      src/db/seed.js
      src/services/user-admin.js
      src/services/salesforce-sync.js
  - Salesforce credentials are server-only. Never add VITE_ prefix to them.
  - Never add the service role key or any secret to a VITE_ variable.

How env vars are consumed:
  - Browser (app):      src/lib/supabase.js reads VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY
  - Seed script:        src/db/seed.js reads SUPABASE_SERVICE_ROLE_KEY via process.env
  - User invites:       src/services/user-admin.js — SUPABASE_SERVICE_ROLE_KEY (server fn)
  - Salesforce lookup:  src/services/salesforce.js — SALESFORCE_* vars (server fn)
  - Salesforce sync:    src/services/salesforce-sync.js — SALESFORCE_* + SERVICE_ROLE_KEY (server fn)
  - LinkedIn summary:   src/services/linkedin-summary.js — GEMINI_API_KEY or OPENAI_API_KEY (server fn)
  - AI field mapping:   src/services/ai-mapping.js — GEMINI_API_KEY (server fn, not yet built)


────────────────────────────────────────────────────────────────────────────────
CURRENT EDITION SUMMARY - PHASE 4.5
--------------------------------------------------------------------------------
Phase 4 added user administration and invite-based login setup:
- /users: Head of KAM can list/create/role-change/activate users.
- /set-password: Invite recipients set their password after clicking the email.
- src/services/user-admin.js: server fn using SUPABASE_SERVICE_ROLE_KEY for Auth Admin.
- src/services/db.js: fetchUsers(), createUserProfile(), updateUserRole(), updateUserStatus().
- src/db/add-user-management.sql: is_active, Head of KAM RPC, profile RLS.
- src/db/rename-ceo-role.sql: migrates legacy C Level enum back to CEO.
- Contracts page: null contract_compliance crash fixed, contract_details array/object handled.

Phase 4.5 adds Salesforce integration:
- src/services/salesforce.js: lookupSalesforceAccountBundle (createServerFn POST)
    Searches Salesforce for an Account by exact name, then fetches related Contacts.
    Auth: OAuth Client Credentials flow (CLIENT_ID + CLIENT_SECRET only).
    Falls back to static access token env var, then sf CLI (last resort, local only).
    Returns: { found, account, contacts, formattedText, matchType }
    If not found: { found: false, message: "Account not found..." }
- src/services/salesforce-sync.js: syncSalesforceMappedFieldsServer (createServerFn POST)
    Validates user session via anon key, then uses service role key to write.
    Whitelists allowed account columns (ACCOUNT_SYNC_COLUMNS set).
    Whitelists allowed stakeholder columns (STAKEHOLDER_SYNC_COLUMNS set).
    Upserts stakeholders: matches by email first, name second; inserts if no match.
- accounts.$accountId.jsx OverviewTab:
    "Sync from Salesforce" button triggers lookupSalesforceAccountBundle.
    On found: opens SalesforceMappingModal with pre-built mapping rows.
    buildSalesforceMappingRows(): maps SF account fields → KYC fields,
    and creates one row-group per SF Contact → Stakeholder fields.
    finalizeSalesforceRows(): marks canSync=false if SF value is empty,
    marks defaultSelected=true if SF value differs from current value.
- SalesforceMappingModal (inline component in accounts.$accountId.jsx):
    Table columns: Sync ✓ | Our field | Current value | SF field | SF value.
    Grouped by: Account/KYC, then one section per Contact.
    Checkbox per row; disabled when SF value is empty (canSync=false).
    Select All / Clear buttons.
    Error display in footer. Sync button with spinner.
    On sync: calls syncSalesforceMappedFields (db.js) → syncSalesforceMappedFieldsServer.
    Writes to account_history after sync via logAccountChanges().
    Updates local fields state + savedSnapshot so UI reflects synced values immediately.

Salesforce auth decision (important):
  The org (orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com) is an Agentforce/OrgFarm
  org that blocks the OAuth Username-Password flow at the infrastructure level.
  Client Credentials flow is used instead — no username/password in env vars.
  The Connected App requires:
    - "Enable Client Credentials Flow" checked (Setup → App Manager → Edit)
    - "Run As" user set (Setup → App Manager → Manage → Edit Policies)
  This is a one-time Salesforce admin configuration, not a code change.


3. SUPABASE PROJECT
────────────────────────────────────────────────────────────────────────────────
Provider:      Supabase (free tier)
Project URL:   https://ogkxkfhigejggnoobypk.supabase.co
Dashboard:     https://supabase.com/dashboard/project/ogkxkfhigejggnoobypk
Database:      PostgreSQL (hosted by Supabase)
Auth:          Supabase Auth (email + password, signInWithPassword)
Storage:       Not used
Realtime:      Not used (yet)

Connection from app:
  File: src/lib/supabase.js
  -------------------------------------------------------
  import { createClient } from "@supabase/supabase-js";
  const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
  const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
  export const supabase = createClient(supabaseUrl, supabaseAnonKey);
  -------------------------------------------------------
  This single client instance is imported everywhere in the app.


────────────────────────────────────────────────────────────────────────────────
4. DATABASE SCHEMA
────────────────────────────────────────────────────────────────────────────────
Full schema file: src/db/schema.sql

14 TABLES (13 original + 1 added via migration):

┌──────────────────────────┬─────────────────────────────────────────────────┐
│ Table                    │ Description                                     │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ profiles                 │ App users: id (UUID), name, initials, role,     │
│                          │ email                                           │
│ accounts                 │ Client accounts (core table, ~40 columns).      │
│                          │ id is TEXT (e.g. "starlight"), not UUID         │
│ stakeholders             │ Contacts at each account                        │
│ health_scores            │ One row per (account, health_area) with score   │
│                          │ + kpi_data jsonb (added by add-kpi-data.sql)    │
│ health_metrics           │ Individual metric rows under each health_score  │
│ contract_details         │ Contract specifics + SWOT + resource fields     │
│ activities               │ Tasks/activities per account (area, RAG, owner) │
│ retention_growth         │ Services: delivered/offered/white-space/N/A     │
│ education_log            │ Education history per account                   │
│ escalations              │ Escalation tickets with priority + SLA          │
│ escalation_action_items  │ Sub-tasks under each escalation                 │
│ opportunities            │ Growth opportunities surfaced for accounts      │
│ notifications            │ In-app notification records                     │
│ account_history          │ Audit log: field changes per account            │
│                          │ (added by add-account-history.sql)              │
└──────────────────────────┴─────────────────────────────────────────────────┘

account_history columns:
  id          uuid  DEFAULT gen_random_uuid() PRIMARY KEY
  account_id  text  REFERENCES accounts(id) ON DELETE CASCADE  ← TEXT not UUID
  field_name  text  NOT NULL
  old_value   text  (nullable)
  new_value   text  (nullable)
  edited_by   text  NOT NULL  ← display name string, not a UUID FK
  edited_at   timestamptz  DEFAULT now()

health_scores additional column (added by migration):
  kpi_data    jsonb  (stores full KpiSection[] array for KPI Editor modal)

accounts additional column (added by migration):
  assigned_kam_id  text  REFERENCES profiles(id)  ← which KAM owns this account
  linkedin_url     text  Salesforce Account Company_LinkedIn__c mapped into Aether KAM
  linkedin_summary text  AI-generated Account's Linkedin Summary
  linkedin_summary_updated_at timestamptz  Last successful summary generation time
  website_url      text  Company website URL used for Account's Website Summary
  website_summary  text  AI-generated Account's Website Summary
  website_summary_updated_at timestamptz  Last successful website summary generation time

profiles additional column (added by add-user-management.sql and now in schema):
  is_active  boolean  DEFAULT true
  Inactive users are signed out by AuthContext and excluded from KAM assignment.

ENUMS (PostgreSQL custom types):
  account_status    → 'healthy' | 'at-risk' | 'critical'
  retention_risk    → 'Low' | 'Medium' | 'High'
  contract_type     → 'Staff Augmented' | 'Time Based' | 'Retainer' | 'Project'
  account_tier      → 'Enterprise' | 'Growth' | 'Strategic'
  influence_type    → 'Champion' | 'Decision Maker' | 'Influencer' | 'Blocker'
  health_area       → 'relationship' | 'project' | 'white_space' | 'contract'
                      | 'csat' | 'risk' | 'resource' | 'financial'
  activity_area     → 'Relationship' | 'Project' | 'Resource' | 'Financial' | 'Profit'
  activity_status   → 'Open' | 'In Progress' | 'Done'
  rag_status        → 'R' | 'A' | 'G'
  priority_level    → 'P1' | 'P2' | 'P3'
  notif_type        → 'action' | 'alert' | 'info'
  confidence_level  → 'Low' | 'Medium' | 'High'
  user_role         → 'CEO' | 'Head of KAM' | 'KAM'

IMPORTANT TYPE NOTE:
  accounts.id is TEXT (values like "starlight", "axiom", not UUIDs).
  Any table with an FK to accounts.id must use TEXT, not uuid.
  This affects: account_history.account_id (TEXT).
  All other FKs (stakeholders, health_scores, etc.) also use TEXT for account_id.


────────────────────────────────────────────────────────────────────────────────
5. ROW LEVEL SECURITY (RLS)
────────────────────────────────────────────────────────────────────────────────
RLS is ENABLED on all tables.
Read policies:  public SELECT (anon + authenticated can read all rows)
Write policies: authenticated UPDATE on accounts, health_scores, health_metrics
                authenticated INSERT on account_history
                authenticated profile insert/update only when current user is
                an active Head of KAM (add-user-management.sql)

How to enable RLS + add read policies:
  Run src/db/enable-rls.sql in Supabase SQL Editor.

Write policies are added by the migration scripts:
  add-kam-assignment.sql              → UPDATE policy on accounts
  add-kpi-data.sql                    → UPDATE policies on health_scores + health_metrics
  add-account-history.sql             → SELECT + INSERT policies on account_history
  add-account-insert-policy.sql       → INSERT policy on accounts
  add-health-scores-insert-policy.sql → INSERT policy on health_scores

Additional Phase 4 scripts:
  add-user-management.sql             -> profile user-management RPCs and RLS
  rename-ceo-role.sql                 -> migrate legacy C Level enum back to CEO

Without read policies: anon key returns 0 rows (blocked by RLS).
Without RLS at all: Supabase shows critical security warning in dashboard.


────────────────────────────────────────────────────────────────────────────────
6. SQL SCRIPTS (src/db/)
────────────────────────────────────────────────────────────────────────────────
schema.sql
  Purpose:  Create all 13 original tables, enums, and constraints.
  Run:      Supabase SQL Editor. Once per fresh setup.

reset.sql
  Purpose:  Drop all tables and enums in correct dependency order.
  WARNING:  Destroys all data. Dev/reset only.

enable-rls.sql
  Purpose:  Enable RLS + add public SELECT policy on every table.
  Run:      Once after schema.sql.

grant-read.sql
  Purpose:  Grant SELECT privilege to anon + authenticated roles.
  Run:      Once after enable-rls.sql.

seed.js
  Purpose:  Populate DB with all mock data (idempotent via upsert).
  Run:      bun run seed  (requires SUPABASE_SERVICE_ROLE_KEY in .env.local)
  Inserts:  3 profiles, 5 accounts, 11 stakeholders, 40 health_scores,
            ~150 health_metrics, 5 contract_details, 14 activities,
            20 retention_growth, 6 education_log, 3 escalations,
            8 escalation_action_items, 5 opportunities, 5 notifications

setup-auth.sql
  Purpose:  Link real Supabase Auth users to profile rows.
  Run:      After creating auth users in Supabase Dashboard.
  Does:
    1. Installs trigger on auth.users (auto-creates profile on signup)
    2. UPDATE statements for all 3 users:
         julian@aether.io  → Julian Drake,  Head of KAM
         maya@aether.io    → Maya Lin,      KAM
         bashair@aether.io → Bashair Ahmad, KAM

add-kam-assignment.sql
  Purpose:  Adds assigned_kam_id TEXT column to accounts.
            Sets initial KAM assignments.
            Adds RLS UPDATE policy on accounts.
  Run:      Once. Then re-run bun run seed to populate assigned_kam_id.
  Assignments seeded:
    Julian Drake (Head of KAM): axiom, cloudpeak, northwind
    Maya Lin (KAM):             starlight, vertex
    Bashair Ahmad:              unassigned (assign via UI)

add-kpi-data.sql
  Purpose:  Adds kpi_data jsonb column to health_scores.
            Adds RLS UPDATE policies on health_scores + health_metrics.
  Run:      Once. Enables KPI Editor to persist checkbox/weight state.

add-account-history.sql
  Purpose:  Creates account_history audit log table.
            Adds RLS SELECT + INSERT policies.
  Run:      Once. Enables Client History tab to show change log.
  NOTE:     account_id is TEXT (not uuid) to match accounts.id type.

add-account-work-history.sql
  Purpose:  Adds triggers that write account work changes into account_history.
            Tracks activities, escalations, escalation action items,
            retention/growth services, education log, and opportunities.
  Run:      Once after add-account-history.sql. Safe to rerun.
  NOTE:     This tracks future inserts/updates/deletes after the trigger is installed.

add-account-insert-policy.sql
  Purpose:  Adds INSERT RLS policy on accounts table.
  Run:      Once. Required for Head of KAM to create new accounts via UI.
  Policy:   authenticated users can INSERT — UI enforces Head of KAM restriction.

add-health-scores-insert-policy.sql
  Purpose:  Adds INSERT RLS policy on health_scores table.
  Run:      Once. Required for KPI Editor to save data on brand-new accounts
            that have no existing health_scores rows.


────────────────────────────────────────────────────────────────────────────────
add-user-management.sql
  Purpose:  Enables the /users page.
  Run:      Once after schema/role migrations, then re-run when this file changes.
  Does:
    1. Adds profiles.is_active boolean default true
    2. Installs current_user_is_head_of_kam() permission RPC
    3. Installs normalized_profile_role() helper
    4. Installs create/update profile RPCs used by the UI
    5. Grants authenticated profile insert/update only through Head of KAM policy
  Note:     Supabase Auth users are created by src/services/user-admin.js, not by
            this SQL file. This SQL manages profile rows and permission checks.

rename-ceo-role.sql
  Purpose:  Keeps the app role enum value as CEO.
  Run:      Once if the database previously used C Level.
  Does:     Renames C Level enum value to CEO when possible, or updates profile
            rows from C Level to CEO when both enum values already exist.


7. DATA SERVICE LAYER (src/services/db.js)
────────────────────────────────────────────────────────────────────────────────
All Supabase queries and mutations are centralised here.
Components never call supabase directly.

READ functions:

  fetchAccounts(opts?)
    → Returns FlatAccount[] filtered by role/userId
    → opts: { role, userId } — KAM role filters by assigned_kam_id
    → Used on: dashboard, accounts list, contracts, strategy pages

  fetchAccount(id)
    → Returns full Account shape (all nested data) or null
    → Uses Promise.all for 7 parallel queries
    → Used via TanStack Router loader in accounts.$accountId.jsx

  fetchContracts(opts?)
    → Returns ContractRow[] — accounts joined with contract_details
    → Adds: duration, autoRenew, nonTerminator, priceHike fields
    → Role filtering same as fetchAccounts
    → Used on: contracts page

  fetchKamUsers()
    → Returns KAM-role profiles only (for assignment dropdown)
    → Excludes Head of KAM — can only assign to KAMs

  fetchUsers()
    -> Returns all profile rows for the /users page
    -> Maps role values through normalizeRole() so legacy C Level displays as CEO
    -> Includes isActive and createdAt fields

  fetchEscalations(accountId?)
    → Returns Escalation[] with nested escalation_action_items
    → Used on: escalations page + account detail Escalation tab

  fetchOpportunities(accountId?)
    → Returns Opportunity[]
    → Used on: account detail Activity tab

  fetchNotifications()
    → Returns Notification[]
    → Not yet wired to sidebar (sidebar still uses static mock data)

  fetchAccountHistory(accountId)
    → Returns HistoryEntry[] ordered by edited_at DESC
    → Used on: account detail Client History tab

WRITE functions:

  updateAccountKam(accountId, kamId)
    → Updates accounts.assigned_kam_id

  updateAccountKyc(accountId, updates)
    → Updates KYC columns on accounts table
    → fields: industry, business_info, client_history, revenue, mrr_arr,
              primary_contact_name, engagement_tenure, team_size,
              competitors (array), main_business_flow, linkedin_url, website_url,
              linkedin_summary, website_summary

  updateHealthBlock(accountId, area, score, metricUpdates, kpiData)
    → UPSERTS health_scores: INSERTs if no row exists, UPDATEs if one does
    → Uses maybeSingle() — never throws on missing rows (handles new accounts)
    → Updates each health_metrics row by UUID (skipped if metricUpdates is empty)
    → area values: 'relationship' | 'project' | 'white_space' | 'csat' | 'risk' | 'financial'

  createAccount(data)
    → INSERTs a new row into accounts table
    → Required fields: id (slug), name, shortCode, industry, region, tier,
      contractType, contractValue, arr, renewalDays
    → Optional: primaryContactName, assignedKamId
    → Auto-set defaults: health=50, trend=0, status='healthy', retention_risk='Low',
      growth_upside=0, white_space_count=0, is_startup=false, last_touch='Just now'
    → Requires INSERT RLS policy (add-account-insert-policy.sql)

  createUserProfile(user)
    -> Creates a real Supabase Auth user through createManagedAuthUser()
    -> Sends a Supabase invite email to the user's email address
    -> Upserts the matching profiles row using the Auth user id
    -> Requires SUPABASE_SERVICE_ROLE_KEY on the server and add-user-management.sql

  updateUserRole(userId, role)
    -> Updates the profile role via update_managed_profile_role() RPC
    -> Falls back to direct profiles update only if the RPC is missing

  updateUserStatus(userId, isActive)
    -> Activates/deactivates profiles.is_active via update_managed_profile_status() RPC
    -> Inactive users are signed out by AuthContext

  logAccountChanges(accountId, changes[], editedBy)
    → Inserts rows into account_history
    → changes: Array<{ field: string, oldValue: string, newValue: string }>
    → editedBy: profile display name (e.g. "Julian Drake")
    → Called alongside every write operation in the same mutationFn

Mapper functions:
  mapFlatAccount()  — snake_case DB row → camelCase FlatAccount object
  mapHealthBlock()  — health_scores + health_metrics rows → HealthBlock shape
  block() helper in fetchAccount — when health_metrics is empty but kpi_data exists,
    derives metric bars from KPI sections (score = checked weight ÷ total × 10).
    Derived metrics have id "derived-N" — display only, not updatable by UUID.


────────────────────────────────────────────────────────────────────────────────
8. AUTHENTICATION (src/context/AuthContext.jsx + src/services/auth.js)
────────────────────────────────────────────────────────────────────────────────
Auth provider:  Supabase Auth (email + password)
Auth method:    supabase.auth.signInWithPassword({ email, password })

AuthContext provides:
  session  → Supabase session object (null if logged out)
  profile  → Row from profiles table { id, name, initials, role, email, isActive }
  loading  → true until first getSession() resolves

Profile loading (loadProfile):
  1. Try: SELECT * FROM profiles WHERE id = <auth user UUID>
  2. If not found: SELECT * FROM profiles WHERE email = <auth email>
  Fallback handles mismatch between seeded static UUIDs and real auth UUIDs.
  If profile.is_active is false, AuthContext signs the user out immediately.

Route protection (AppShell in __root.jsx):
  - Auth pages: /login and /set-password render without sidebar
  - Unauthenticated + not on an auth page → navigate('/login')
  - Authenticated + on /login → navigate('/')
  - Authenticated + on /set-password remains on /set-password so invited users
    can save their password
  - While loading → full-screen spinner

Invite/password setup:
  - Head of KAM creates a user from /users.
  - src/services/user-admin.js calls supabase.auth.admin.inviteUserByEmail()
    on the server using SUPABASE_SERVICE_ROLE_KEY.
  - Supabase sends the invite email.
  - Invite link redirects to /set-password.
  - /set-password calls supabase.auth.updateUser({ password }).

Sign out:
  AppSidebar → LogOut button → signOut() → supabase.auth.signOut()
  → AuthContext onAuthStateChange clears session + profile


────────────────────────────────────────────────────────────────────────────────
9. ACCOUNT DETAIL PAGE — 7 TABS (accounts.$accountId.jsx)
────────────────────────────────────────────────────────────────────────────────
Tab 1: Overview (KYC)
  - 10 editable KYC fields + Assigned KAM section
  - Fixed save bar at screen bottom (appears when dirty, auto-hides 3s after save)
  - Discard resets to last saved snapshot
  - Save calls updateAccountKyc() + logAccountChanges()
  - Assigned KAM: dropdown for Head of KAM, read-only for KAM
  - KAM assignment calls updateAccountKam() + logAccountChanges()
  - OCR Auto-fill: UI preview only (not wired to real OCR yet)

Tab 2: Score Marking Matrices
  - Shows 8 health blocks (relationship, project, white_space, contract,
    csat, risk, resource, financial)
  - Each expandable via KPI Editor Modal
  - KPI Editor: weighted checkbox scoring (checked weights ÷ total × 5)
  - New accounts get one default section seeded so modal is never blank
  - "Add KPI section" button in footer — adds a new blank section
  - Delete section button (Trash2) in each section header — removes that section
  - Save & Close calls updateHealthBlock() (upserts) + logAccountChanges()
  - Cancel closes without saving
  - Save errors shown inline in modal footer (previously silent)

Tab 3: Activity to Increase Score
  - Opportunities list (from DB via useQuery)
  - RAG analysis grid
  - Activities table by area
  - All edit buttons disabled until write operations wired for this tab

Tab 4: Retention VS Growth
  - Services: delivered / in-flight / white-space / not applicable
  - Read-only (write operations not yet wired)

Tab 5: Educate Client
  - Relationship improvement suggestions
  - Education history log
  - WhatsApp message button (UI only)

Tab 6: Escalation
  - Lists escalations for this account from DB
  - Read-only (write operations not yet wired)

Tab 7: Client History  ← NEW
  - Shows account_history table filtered by account_id
  - Columns: Field | Old Value | New Value | Edited By | Time
  - Time shown as relative ("5m ago", "2d ago") or formatted date
  - Empty state when no changes exist yet
  - Populated automatically on: KYC save, KAM assignment, KPI score save


────────────────────────────────────────────────────────────────────────────────
10. CONTRACTS PAGE (contracts.jsx)
────────────────────────────────────────────────────────────────────────────────
Fetches data via fetchContracts() which joins:
  accounts (name, tier, contractType, contractValue, renewalDays, contractCompliance)
  + contract_details (duration)
fetchContracts() accepts contract_details as either a Supabase nested array or
single object, then maps contract_details.duration to the Contracts table.

Table columns:
  Account (linked to detail page) | Type | Duration | Value | Renewal | Process Compliance

Renewal badge: red ≤14 days, amber ≤30 days, grey otherwise
Process Compliance: color-coded score (/10)
Role filtering: KAM sees only assigned accounts (same pattern as all list pages)


────────────────────────────────────────────────────────────────────────────────
Null safety: missing values render as "-" so a row such as test-account with
null contract_compliance does not trigger the root "This page didn't load"
error boundary.


11. ROUTING (TanStack Router — file-based)
────────────────────────────────────────────────────────────────────────────────
/           → src/routes/index.jsx          (Dashboard)
/login      → src/routes/login.jsx          (Login — no sidebar)
/accounts   → src/routes/accounts.index.jsx (Accounts portfolio list)
/accounts/:id → src/routes/accounts.$accountId.jsx (Client 360 — 7 tabs)
/contracts  → src/routes/contracts.jsx
/strategy   → src/routes/strategy.jsx
/escalations → src/routes/escalations.jsx
/educate    → src/routes/educate.jsx
__root.jsx  → Root layout (AuthProvider + AppShell)

Additional Phase 4 routes:
  /set-password -> src/routes/set-password.jsx (Invite password setup - no sidebar)
  /users        -> src/routes/users.jsx        (Head of KAM user management)

Account detail uses TanStack Router async loader:
  loader: async ({ params }) => {
    const account = await fetchAccount(params.accountId);
    if (!account) throw notFound();
    return { account };
  }
  Component: Route.useLoaderData() → { account }
  useRouter().invalidate() re-runs the loader after a KPI save

All other pages use useQuery() for client-side fetching.


────────────────────────────────────────────────────────────────────────────────
12. NPM / BUN SCRIPTS
────────────────────────────────────────────────────────────────────────────────
bun run dev       → Start Vite dev server (http://localhost:3000 or similar)
bun run build     → Production build
bun run preview   → Preview production build locally
bun run seed      → Run src/db/seed.js (populate Supabase DB)
bun run lint      → ESLint
bun run format    → Prettier


────────────────────────────────────────────────────────────────────────────────
13. HOW TO SET UP FROM SCRATCH (new machine)
────────────────────────────────────────────────────────────────────────────────
Step 1: Install prerequisites
  - Git: git-scm.com/download/win
  - Node.js 20+: nodejs.org (LTS)
  - Bun: npm install -g bun

Step 2: Clone repo + switch to working branch
  git clone https://github.com/<username>/<repo>.git
  cd <repo>
  git checkout Bashair

Step 3: Install dependencies
  bun install

Step 4: Create .env.local in project root
  Get the full .env.local contents from the team lead via password manager.
  It contains these variables:

  VITE_SUPABASE_URL=https://ogkxkfhigejggnoobypk.supabase.co
  VITE_SUPABASE_ANON_KEY=<anon key>
  SUPABASE_SERVICE_ROLE_KEY=<service/secret key — server only, no VITE_ prefix>

  SALESFORCE_INSTANCE_URL=https://orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com
  SALESFORCE_LOGIN_URL=https://orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com
  SALESFORCE_CLIENT_ID=<Connected App Consumer Key>
  SALESFORCE_CLIENT_SECRET=<Connected App Consumer Secret>

  GEMINI_API_KEY=<free Gemini API key — get at aistudio.google.com>
  OPENAI_API_KEY=<OpenAI API key, if Gemini is not configured>
  OPENAI_MODEL=gpt-5.4-mini

Step 5: Run dev server
  bun run dev

Step 6: Log in with any seeded user
  julian@aether.io / maya@aether.io / bashair@aether.io

Step 7: Configure Supabase Auth redirect URLs for invite emails
  Supabase Dashboard -> Authentication -> URL Configuration
  Add local redirect:
    http://127.0.0.1:5174/set-password
  Add deployed redirect too:
    https://your-domain.com/set-password

NOTE: No additional Salesforce setup is required for developers.
  The Connected App and Salesforce org are already configured.
  Salesforce integration works as soon as the env vars are in .env.local.

For FRESH Supabase setup (first time only):
  1. SQL Editor: schema.sql → enable-rls.sql → grant-read.sql
  2. Add SUPABASE_SERVICE_ROLE_KEY to .env.local
  3. bun run seed
  4. Create 3 auth users in Supabase: julian@ / maya@ / bashair@ (@aether.io)
  5. SQL Editor: setup-auth.sql
  6. SQL Editor: add-kam-assignment.sql
  7. SQL Editor: add-kpi-data.sql
  8. SQL Editor: add-account-history.sql
  9. SQL Editor: add-account-insert-policy.sql
  10. SQL Editor: add-health-scores-insert-policy.sql
  11. SQL Editor: rename-ceo-role.sql
  12. SQL Editor: add-user-management.sql
  13. SQL Editor: add-account-linkedin-url.sql
  14. SQL Editor: add-linkedin-summary.sql
  15. SQL Editor: add-linkedin-summary-updated-at.sql
  16. SQL Editor: add-account-website-summary.sql


────────────────────────────────────────────────────────────────────────────────
14. SEEDED DATA REFERENCE
────────────────────────────────────────────────────────────────────────────────
5 accounts (TEXT IDs):
  starlight  → Starlight Systems Corp.  (Enterprise, health 84)
  axiom      → Axiom Logistics          (Enterprise, health 88)
  vertex     → Vertex Industrial        (Growth,     health 42)
  cloudpeak  → CloudPeak Systems        (Strategic,  health 76)
  northwind  → Northwind Finance        (Enterprise, health 61)

KAM assignments (set in add-kam-assignment.sql + seed):
  Julian Drake:  axiom, cloudpeak, northwind
  Maya Lin:      starlight, vertex
  Bashair Ahmad: unassigned (assign via the UI)

3 profiles (static UUIDs in seed, matched to real auth users via email fallback):
  00000000-0000-0000-0000-000000000001 → Julian Drake,  Head of KAM, julian@aether.io
  00000000-0000-0000-0000-000000000002 → Maya Lin,      KAM,         maya@aether.io
  00000000-0000-0000-0000-000000000003 → Bashair Ahmad, KAM,         bashair@aether.io

3 escalations:
  esc-001 → Starlight: API Latency West-2 Cluster (P1)
  esc-002 → Axiom: Latent DB Indexing Failure (P1)
  esc-003 → Vertex: Unrealistic SLA Renegotiation (P2)


────────────────────────────────────────────────────────────────────────────────
15. SALESFORCE INTEGRATION
────────────────────────────────────────────────────────────────────────────────
Salesforce Org:
  URL:      https://orgfarm-d3e5f8291b-dev-ed.develop.my.salesforce.com
  Type:     Agentforce / OrgFarm Developer org
  Username: ahmad.h.06088b0449bf@agentforce.com

Connected App: "Aether KAM Integration"
  Auth flow:  OAuth Client Credentials (grant_type=client_credentials)
  Why:        This org blocks the Username-Password flow at infrastructure level.
              Client Credentials flow requires no user credentials in env vars.
  Required Salesforce config (one-time admin step, already done):
    - "Enable Client Credentials Flow" checked on the Connected App
    - "Run As" user set under App Manager → Manage → Edit Policies

Auth flow in code (src/services/salesforce.js getSalesforceAuth()):
  Priority 1: OAuth Client Credentials
    Uses: SALESFORCE_INSTANCE_URL + SALESFORCE_LOGIN_URL + CLIENT_ID + CLIENT_SECRET
    Token endpoint: {SALESFORCE_LOGIN_URL}/services/oauth2/token
    Gets a fresh token on every call — never expires mid-session.
  Priority 2: Static access token (legacy fallback)
    Uses: SALESFORCE_INSTANCE_URL + SALESFORCE_ACCESS_TOKEN
    Not recommended — tokens expire after a few hours.
  Priority 3: sf CLI (last resort)
    Runs: sf org display -o kamDummy --verbose --json
    Only works on machines with Salesforce CLI installed and authenticated.
    Will fail in production and on other developers' machines.

What gets fetched (salesforce.js):
  Account fields: 40+ fields including Industry, AnnualRevenue, NumberOfEmployees,
    BillingCity/State/Country, Description, Services_Offered__c, Target_Audience__c,
    Revenue_Generation_Model__c, Funding_Stage__c, Previous_Communication_Needs__c,
    KYC_Research_Notes__c, Current/Previous_Salesforce_Platform__c, Tech_Stack_Notes__c,
    SaaS_Platforms_Used__c, and more.
  Contact fields: Up to 20 contacts per account — Name, Email, Title, Decision_Maker__c,
    Primary_KYC_Contact__c, Prospect_Persona__c, and more.
  Search method: Exact name match SOQL query (case-sensitive) → "not found" if no match.

What gets written (salesforce-sync.js):
  Account fields whitelist (ACCOUNT_SYNC_COLUMNS):
    industry, business_info, client_history, revenue, primary_contact_name,
    primary_contact_role, region, employees, main_business_flow, linkedin_url,
    website_url
  Stakeholder fields whitelist (STAKEHOLDER_SYNC_COLUMNS):
    name, role, email, influence, last_contact
  Stakeholder matching: email first → name → insert as new row if no match.
  Uses SUPABASE_SERVICE_ROLE_KEY to bypass RLS for writes.
  All synced fields are logged to account_history table.

AI mapping assistant (PLANNED — not yet built):
  File: src/services/ai-mapping.js (to be created)
  API:  Google Gemini 2.0 Flash — free tier (1500 req/day)
  Key:  GEMINI_API_KEY in .env.local
  Purpose: Given the mapping rows (current value + SF value), recommends which
    rows to pre-check. User can override. Strictly field-mapping only — no other role.
  UI: "✨ AI Suggest" button in SalesforceMappingModal header.
      AI badge next to checkbox on AI-recommended rows.

────────────────────────────────────────────────────────────────────────────────
17. DESIGN TOKENS (Tailwind / CSS custom properties in src/styles.css)
────────────────────────────────────────────────────────────────────────────────
--accent              → primary blue/teal accent color
--success             → green (healthy, positive)
--warn                → amber (medium risk, warnings)
--crit                → red (critical, errors, P1)
--sidebar             → dark sidebar background
--card                → card/panel background
--background          → page background
--foreground          → primary text
--muted-foreground    → secondary text

Fonts (Google Fonts):
  Inter          → UI text
  JetBrains Mono → Monospace (codes, IDs, timestamps)


================================================================================
END OF PROJECT DOCUMENTATION
================================================================================
